# voiranime-scraping

## Streamtape extraction

- Run: `python main.py <streamtape_embed_url>`
- Prints the direct video URL resolved from Streamtape.

## Tests

- Run: `python -m unittest tests/test_streamtape.py`
